import { ItemFacturaRequest } from './item-factura-request';

describe('ItemFacturaRequest', () => {
  it('should create an instance', () => {
    expect(new ItemFacturaRequest()).toBeTruthy();
  });
});
